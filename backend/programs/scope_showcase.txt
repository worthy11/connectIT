METHOD block2(LAYER r) RETURNS LAYER [
    r <+-> *blue*
    RETURN r
]

METHOD block1(LAYER r) RETURNS LAYER [
    UNIT u
    r <+-> *red*
    r = PERFORM block2(r)
    RETURN r
]

LAYER a = [[*green*]]
a = PERFORM block1(a, a)
SHOW a